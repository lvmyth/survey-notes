// 文章：cv-survey
ARTICLE_CONTENTS["cv-survey"] = "# 让全站仪长眼睛——计算机视觉在测量中的应用\n\n> 全站仪再贵，也要人瞄准。如果仪器自己能「看到」棱镜、自动锁定目标呢？计算机视觉就是给仪器装上一双眼睛——识别棱镜、读取编码、定位标志点。这双眼睛不仅不会累，精度还比人眼高。\n\n## 一、为什么要给全站仪装眼睛？\n\n传统全站仪操作中，最耗时的环节是什么？**找目标和瞄准**。\n\n- 测量员转动仪器，透过目镜搜索棱镜\n- 在大太阳底下眯着眼睛对焦\n- 夜间施工还需要照明辅助\n\n计算机视觉可以解决这个问题：\n\n- **自动识别棱镜**：相机拍摄画面，AI 在图像中定位棱镜位置\n- **自动跟踪**：棱镜移动时，视觉算法实时跟踪，伺服电机自动转动\n- **读码识别**：识别测量标志上的二维码，自动获取点号信息\n- **免棱镜测量辅助**：在图像上点选目标，自动测量\n\n## 二、用 OpenCV 识别棱镜\n\n棱镜在图像中通常呈现为**圆形反光区域**。\n\n```python\nimport cv2\nimport numpy as np\n\ndef find_prism(image_path):\n    img = cv2.imread(image_path)\n    gray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)\n\n    # 边缘检测\n    edges = cv2.Canny(gray, 50, 150)\n\n    # 找圆形轮廓\n    circles = cv2.HoughCircles(gray, cv2.HOUGH_GRADIENT,\n                               dp=1.2, minDist=50,\n                               param1=100, param2=30,\n                               minRadius=10, maxRadius=100)\n\n    if circles is not None:\n        circles = np.round(circles[0, :]).astype(int)\n        for (x, y, r) in circles:\n            cv2.circle(img, (x, y), r, (0, 255, 0), 2)\n            cv2.circle(img, (x, y), 2, (0, 0, 255), 3)\n            print(f\"棱镜中心: ({x}, {y}), 半径: {r} px\")\n            return (x, y, r)\n    return None\n```\n\n这个算法可以在图像中自动找到棱镜位置，返回像素坐标。再通过相机标定参数，就可以换算成仪器的水平和垂直角。\n\n## 三、二维码定位测量\n\n在测量标志上贴一个二维码，相机拍摄后自动解码，获取点号信息。\n\n```python\nfrom pyzbar.pyzbar import decode\n\ndef read_qr(image_path):\n    img = cv2.imread(image_path)\n    decoded = decode(img)\n\n    for obj in decoded:\n        points = obj.polygon\n        data = obj.data.decode(\"utf-8\")\n        print(f\"解码数据: {data}\")\n\n        # 绘制二维码边界\n        if len(points) == 4:\n            pts = [(p.x, p.y) for p in points]\n            cv2.polylines(img, [np.array(pts, np.int32)],\n                          True, (0, 255, 0), 2)\n\n        return data\n    return None\n```\n\n### 应用场景\n\n- 在控制点标志上贴二维码，扫描直接识别点号和坐标\n- 在放样点位上贴码，放样时自动确认是否到达目标位置\n- 在仪器脚架位置做标记，快速恢复设站\n\n## 四、模板匹配：找特定目标\n\n```python\ndef find_target(image_path, template_path):\n    img = cv2.imread(image_path)\n    template = cv2.imread(template_path)\n    h, w = template.shape[:2]\n\n    # 模板匹配\n    result = cv2.matchTemplate(img, template, cv2.TM_CCOEFF_NORMED)\n    _, max_val, _, max_loc = cv2.minMaxLoc(result)\n\n    if max_val > 0.8:  # 匹配度大于 0.8\n        top_left = max_loc\n        bottom_right = (top_left[0] + w, top_left[1] + h)\n        cv2.rectangle(img, top_left, bottom_right, (0, 255, 0), 2)\n        center = (top_left[0] + w // 2, top_left[1] + h // 2)\n        print(f\"目标中心: {center}, 置信度: {max_val:.3f}\")\n        return center\n    return None\n```\n\n模板匹配适合识别**固定形状的标志**——比如特定款式的棱镜、测量标靶。\n\n## 五、实战：自动瞄准模拟\n\n把以上功能整合，模拟全站仪自动瞄准流程：\n\n```python\nclass AutoTargeting:\n    def __init__(self, camera_id=0):\n        self.cap = cv2.VideoCapture(camera_id)\n        self.target_pos = None\n\n    def find_and_track(self):\n        while True:\n            ret, frame = self.cap.read()\n            if not ret:\n                break\n\n            # 找棱镜\n            prism = find_prism_in_frame(frame)\n            if prism:\n                x, y, r = prism\n                self.target_pos = (x, y)\n\n                # 计算偏移量（相对于画面中心）\n                h, w = frame.shape[:2]\n                cx, cy = w // 2, h // 2\n                dx = x - cx\n                dy = y - cy\n\n                cv2.putText(frame, f\"偏移: ({dx:+d}, {dy:+d}) px\",\n                            (10, 30), cv2.FONT_HERSHEY_SIMPLEX,\n                            0.7, (0, 255, 0), 2)\n\n                # 如果偏移小于阈值，认为已瞄准\n                if abs(dx) < 10 and abs(dy) < 10:\n                    cv2.putText(frame, \"已瞄准!\", (10, 60),\n                                cv2.FONT_HERSHEY_SIMPLEX,\n                                0.7, (0, 0, 255), 2)\n\n            cv2.imshow(\"自动瞄准\", frame)\n            if cv2.waitKey(1) & 0xFF == ord(\"q\"):\n                break\n\n        self.cap.release()\n        cv2.destroyAllWindows()\n```\n\n这个 Demo 展示了计算机视觉如何实现自动瞄准——找到目标 → 计算偏移 → 调整角度 → 锁定目标。实际的测量机器人（如 Leica Nova 系列）就是基于类似的原理。\n\n## 六、计算机视觉 + 测量的其他应用\n\n| 应用 | 原理 | 精度 |\n|------|------|------|\n| 自动瞄准棱镜 | Hough找圆 + 模板匹配 | 亚像素级 |\n| 二维码测量标志 | 二维码解码 + 四点定位 | 毫米级 |\n| 裂缝宽度测量 | 边缘检测 + 像素标定 | 0.1mm |\n| 位移监测 | 数字图像相关 (DIC) | 0.01mm |\n| 变形监测 | 特征点跟踪 + 差分分析 | 亚毫米级 |\n\n## 七、需要什么基础\n\n```\n# 安装依赖\npip install opencv-python numpy pyzbar\n```\n\n**硬件**：一个 USB 摄像头或工业相机，装在仪器上或独立架设。\n\n就这些。不需要 GPU，不需要深度学习，传统的计算机视觉算法就能解决很多测量问题。\n\n## 总结\n\n计算机视觉给测量仪器装上了一双「不会累的眼睛」：\n\n- **看**——用相机拍摄，OpenCV 处理图像\n- **找**——Hough 变换找圆、模板匹配找目标、QR 码读信息\n- **瞄**——计算偏移量，驱动伺服电机\n- **测**——通过像素换算实现非接触测量\n\n测量的本质是获取位置信息，而图像里包含了最丰富的位置信息。从图像中提取测量信息，就是计算机视觉在测量中最直接的价值。\n";
