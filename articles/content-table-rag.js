// 文章：table-rag
ARTICLE_CONTENTS["table-rag"] = "# 表格 RAG：让 AI 会查测量数据表\n\n> 测量工作天天和数据表打交道：导线记录表、水准观测手簿、逐桩高程表、断面填挖方表。这些表格有两大特点——**数字密集**（桩号、坐标、高程、偏差）和**结构规整**（行是记录、列是字段）。把它们交给普通 RAG（文档切块 + 向量检索）效果很差：表格一切块就散了架，embedding 对\"K1+100\"\"45231.125\"这类数字无能为力，向量检索也分不清\"查询 K1+100\"和\"查询 K1+200\"差在哪。本文是 RAG 系列的第四篇，专门解决\"让 AI 查表格\"的问题：先讲清为什么普通 RAG 查不好表格，再对比表格问答的三种技术路线（Text2SQL、表格向量化、混合路由），给出用 SQLite + 本地 LLM 构建测量数据问答系统的完整代码，最后讲评估与防坑。\n\n## 一、为什么普通 RAG 查不好表格\n\n### 1.1 表格切块的两个致命伤\n\n普通 RAG 把文档切成文本块再向量化，对表格会遇到：\n\n```text\n原始表格（导线记录）：\n桩号      X坐标        Y坐标      高程\nK1+100   500.000     300.000    45.200\nK1+120   500.245     300.112    45.350\nK1+140   500.489     300.225    45.500\n\n切成文本块后：\n\"K1+100   500.000     300.000    45.200\"   ← 一行一坨，语义破碎\n```\n\n- **行被拆散**：表头（字段名）和行数据分离，向量化后不知道\"500.000\"是 X 坐标还是别的\n- **数字无语义**：embedding 模型对数字的区分度极差——\"45.200\"和\"45.300\"在向量空间几乎一样，检索\"K1+120 的高程\"可能返回 K1+100 的行\n\n### 1.2 表格问答的真实需求\n\n测量里的表格问题大多是**精确查询**：\n\n- \"K1+120 的设计高程是多少？\" → 查一行，取一列\n- \"3 号导线点的坐标？\" → 查一行，取两列\n- \"这段路的最大填挖值出现在哪？\" → 聚合查询\n\n这类问题**答案唯一、必须精确**——恰好是向量检索最不擅长的，却是数据库（SQL）最擅长的。\n\n> 注意：普通 RAG 适合\"哪段文档讲了什么\"（找段落），表格问答是\"某行某列的值\"（查记录）。两者的技术路线不同，硬用一套会两头不讨好。\n\n---\n\n## 二、表格问答的三种技术路线\n\n| 路线 | 原理 | 优点 | 缺点 | 适用 |\n|------|------|------|------|------|\n| **Text2SQL** | 自然语言→SQL→执行 | 精确、支持聚合 | 需 LLM 生成 SQL、有出错风险 | 结构化大表、精确数值查询 |\n| **表格向量化** | 表转文本块再向量检索 | 实现简单、可混合 | 数值精度差 | 小表、\"哪张表有这内容\"的定位 |\n| **混合路由** | 先判断问题类型再分流 | 各取所长 | 需要一个路由判断 | 完整系统 |\n\n**结论**：测量数据问答的正确答案是 **Text2SQL 为主 + 路由判断**——精确数值查询走 SQL，描述性查询（\"规范里怎么说的\"）走普通 RAG。\n\n---\n\n## 三、Text2SQL 原理\n\n### 3.1 流程\n\n```text\n用户问题：\"K1+120 的设计高程是多少？\"\n    ↓\nLLM 生成 SQL（给定表结构描述）\n    ↓\n执行 SQL（SQLite/数据库，只读）\n    ↓\n结果 → 格式化回答：\"K1+120 设计高程为 45.350m\"\n```\n\n关键在第一步：**给 LLM 足够的表结构信息（schema），让它生成正确的 SQL**。\n\n### 3.2 Schema 描述示例\n\n```text\n表 traverse_points（导线点记录）：\n  id        INTEGER  主键\n  station   TEXT     桩号，如 K1+100\n  x         REAL     X 坐标（m）\n  y         REAL     Y 坐标（m）\n  elev      REAL     高程（m）\n```\n\nLLM 拿到这个描述 + 用户问题，就能生成：\n\n```sql\nSELECT elev FROM traverse_points WHERE station = 'K1+120';\n```\n\n### 3.3 为什么本地也能做\n\n生成简单 SQL 不需要大模型——Ollama 上的 7B 模型（如 qwen2.5:7b）足够应付\"SELECT...WHERE...\"级别的查询。复杂聚合（GROUP BY、JOIN）建议用更强模型或人工校验。\n\n---\n\n## 四、实战：测量数据问答系统\n\n### 4.1 环境准备\n\n```bash\npip install ollama   # 或直接用 requests 调本地 Ollama\n```\n\n### 4.2 建表：把测量数据导入 SQLite\n\n```python\nimport sqlite3, csv\n\nconn = sqlite3.connect('survey.db')\ncur = conn.cursor()\n\n# 建导线点表\ncur.execute('''\nCREATE TABLE IF NOT EXISTS traverse_points (\n    id INTEGER PRIMARY KEY,\n    station TEXT,\n    x REAL,\n    y REAL,\n    elev REAL\n)''')\n\n# 导入 CSV（桩号,X,Y,高程）\nwith open('traverse_points.csv', encoding='utf-8') as f:\n    reader = csv.reader(f)\n    next(reader)                      # 跳过表头\n    cur.executemany('INSERT INTO traverse_points (station,x,y,elev) VALUES (?,?,?,?)', reader)\nconn.commit()\n```\n\n### 4.3 Text2SQL 核心函数\n\n```python\nimport json, requests\n\nOLLAMA_URL = 'http://localhost:11434/api/chat'\nMODEL = 'qwen2.5:7b'\n\nSCHEMA = \"\"\"\n表 traverse_points（导线点记录）：\n  station TEXT 桩号，格式 K1+100\n  x REAL X坐标(m)，y REAL Y坐标(m)，elev REAL 高程(m)\n\"\"\"\n\ndef ask_table(question):\n    # 1. LLM 生成 SQL\n    prompt = f\"\"\"你是 SQL 专家。根据表结构，把问题翻译成 SQLite 的 SELECT 查询。\n只输出 SQL，不要解释。表结构：\n{SCHEMA}\n问题：{question}\nSQL：\"\"\"\n    resp = requests.post(OLLAMA_URL, json={\n        'model': MODEL, 'messages': [{'role': 'user', 'content': prompt}], 'stream': False\n    })\n    sql = resp.json()['message']['content'].strip()\n    # 清理可能出现的 markdown 代码块\n    sql = sql.strip('`').replace('```sql', '').replace('```', '').strip()\n\n    # 2. 执行 SQL（只读保护）\n    if not sql.upper().startswith('SELECT'):\n        return '无法生成查询'\n    try:\n        rows = conn.execute(sql).fetchall()\n    except Exception as e:\n        return f'查询失败：{e}'\n\n    # 3. 格式化回答\n    if not rows:\n        return '未找到对应记录'\n    return '\\n'.join(str(r) for r in rows)\n\n# 测试\nprint(ask_table('K1+120 的高程是多少'))\nprint(ask_table('一共有多少个导线点'))\n```\n\n### 4.4 测量场景示例\n\n```text\n问：\"K1+120 的高程是多少？\"\nSQL：SELECT elev FROM traverse_points WHERE station = 'K1+120'\n答：(45.35,)\n\n问：\"X 坐标最大的点是谁？\"\nSQL：SELECT station, x FROM traverse_points ORDER BY x DESC LIMIT 1\n答：('K1+180', 500.955)\n\n问：\"高程大于 45.4 的点有哪些？\"\nSQL：SELECT station, elev FROM traverse_points WHERE elev > 45.4\n答：[('K1+140', 45.5), ('K1+160', 45.6)]\n```\n\n---\n\n## 五、表格向量化（备用方案）\n\n如果不想接 LLM 生成 SQL（或表很小），可以把表格转文本后走向量检索：\n\n```python\n# 表转文本块：每行一条，带表头上下文\ntexts = [\n    '导线点 K1+100 X=500.000 Y=300.000 高程=45.200',\n    '导线点 K1+120 X=500.245 Y=300.112 高程=45.350',\n    # ...\n]\n# 之后按《Faiss向量检索实战》的方法建索引检索\n```\n\n**局限**：检索\"K1+120 高程\"时，向量可能返回 K1+100 或 K1+140 的行——数字区分度差。适合\"哪个点在哪\"这类粗粒度问题，不适合精确取值。\n\n> 注意：表格向量化的正确用法是**定位**（\"哪个表/哪个区段有这些数据\"），精确取值必须交给 SQL。\n\n---\n\n## 六、混合路由：完整测量知识库\n\n把表格问答和文本 RAG 组合成完整系统：\n\n```python\ndef route(question):\n    \"\"\"判断问题类型：表格查询 → SQL；规范/文档 → 文本 RAG\"\"\"\n    # 简单规则路由（也可用 LLM 判断）\n    table_keywords = ['高程', '坐标', '桩号', '填挖', '记录', '表', '多少', '最大', '最小']\n    if any(k in question for k in table_keywords):\n        return ask_table(question)\n    else:\n        return ask_text_rag(question)   # 前几篇文章的普通 RAG\n\nprint(route('K1+120 的高程是多少'))       # → SQL 查询\nprint(route('隧道贯通误差允许值是多少'))   # → 文本 RAG\n```\n\n路由是系统设计的灵魂——**一个入口，两类引擎**：精确数字走数据库，语义内容走向量。\n\n---\n\n## 七、评估与防坑\n\n### 7.1 评估\n\n准备\"问题 → 期望结果\"标注集：\n\n- **SQL 正确率**：LLM 生成 SQL 能否正确执行并返回期望结果\n- **回答准确率**：最终回答与真实值的对比（数值逐位核对）\n- 常见失败模式：SQL 语法错、字段名猜错（把 x 当 y）、桩号格式不匹配（'K1+120' vs 'k1+120'）\n\n### 7.2 防坑清单\n\n| 坑 | 对策 |\n|------|------|\n| LLM 生成非 SELECT 语句（DELETE/UPDATE） | 强制校验开头必须是 SELECT，只读连接 |\n| 字段名猜错 | Schema 描述写清字段含义 + 给 1~2 个示例 |\n| 桩号大小写/格式不统一 | 入库时统一格式（大写 K，+号固定） |\n| SQL 注入（表名/字段名来自用户输入） | 只允许 SELECT、用参数绑定 |\n| 查询结果太多 | 自动 LIMIT 10，并提示 |\n| 列名带单位混淆 | Schema 注明单位（m、mm、%） |\n| 空表/查无记录 | 返回\"未找到\"并提示可用字段 |\n\n---\n\n## 八、小结\n\n表格 RAG 的核心结论一句话：**测量数据问答别用向量检索硬啃，让数据库干它最擅长的活**。\n\n- 普通 RAG 管\"语义段落\"，Text2SQL 管\"精确取值\"，路由把它们接起来\n- 本地 7B 模型生成简单 SQL 完全够用，不依赖云端 API\n- 表格向量化只做定位，不做取值\n- 防坑重点：只读保护、Schema 描述、桩号格式统一、结果限行\n\n把这张\"表\"加进你的测量知识库，AI 助手就能从\"答规范问题\"升级到\"查你的实际数据\"——规范条文 + 现场记录，一个入口全搞定。";
