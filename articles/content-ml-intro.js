// 文章：ml-intro
ARTICLE_CONTENTS["ml-intro"] = "# 什么是机器学习？用测量数据举个例子\n\n> 机器学习没有你想象的那么神秘。从测量员的视角来看，它就是一个「根据历史数据自动总结规律」的工具——和你根据前几期沉降数据判断未来趋势，本质上是一回事。\n\n## 一、机器学习是什么？用测量术语打个比方\n\n想象一个场景：\n\n你是一个有经验的测量员，看到一组沉降观测数据，扫一眼就能判断「这个工地沉降正常，趋于收敛了」。你的判断依据是什么？是过去十年看过的几百个沉降曲线在你脑子里形成的**经验**。\n\n**机器学习做的就是同一件事**——给它足够多的历史数据（经验），它就能学会从中找规律，然后用在新的数据上做预测或判断。\n\n区别在于：\n\n- 你看100条曲线需要十年经验积累\n- 机器看100万条曲线只需要几分钟\n\n## 二、第一个例子：用线性回归预测沉降\n\n回到最熟悉的场景：沉降观测。\n\n### 问题\n\n已知一个建筑物前6期的累计沉降数据，想预测第7期、第8期的沉降值。\n\n### 数据\n\n```python\nimport numpy as np\n\n# 期数（1-6期）\nX = np.array([1, 2, 3, 4, 5, 6])\n# 累计沉降量（mm）\ny = np.array([-1.2, -2.8, -4.1, -5.0, -5.6, -5.9])\n```\n\n### 用机器学习做预测\n\n```python\nfrom sklearn.linear_model import LinearRegression\n\n# 训练模型\nmodel = LinearRegression()\nmodel.fit(X.reshape(-1, 1), y)\n\n# 预测第7期和第8期\nfuture = np.array([[7], [8]])\npred = model.predict(future)\n\nfor i, p in enumerate(pred):\n    print(f'第{i+7}期预测沉降: {p:.2f} mm')\n```\n\n输出结果：\n\n```\n第7期预测沉降: -6.16 mm\n第8期预测沉降: -6.38 mm\n```\n\n这就是一个最简单的机器学习模型。不是靠经验拍脑袋，而是靠数据算出来的。虽然线性回归很简单，但它揭示了你做沉降趋势判断的本质——**从历史数据中找出变化规律，应用到未来**。\n\n### 可视化对比\n\n```python\nimport matplotlib.pyplot as plt\n\n# 历史数据\nplt.plot(X, y, 'o-', label='实测数据', color='#2c6e49')\n# 预测数据\nplt.plot(future, pred, 's--', label='预测趋势', color='#d68c45')\nplt.axhline(y=-6.0, color='red', linestyle=':', label='预警值')\nplt.legend()\nplt.title('沉降趋势预测')\nplt.xlabel('观测期数')\nplt.ylabel('累计沉降 (mm)')\n```\n\n预测曲线趋势，比单看数字直观得多。\n\n## 三、第二个例子：用聚类发现异常测点\n\n除了预测，机器学习还可以用来「找异常」。\n\n### 场景\n\n你有一片场地的50个沉降观测点，需要快速找出那些「沉降异常」的点。\n\n```python\nfrom sklearn.cluster import KMeans\n\n# 模拟数据：每个点的 [累计沉降, 本期速率]\ndata = np.array([\n    [1.2, 0.1], [1.5, 0.2], [8.7, 1.8], [1.3, 0.1],  # 第3个点是异常\n    [1.1, 0.0], [9.2, 2.1], [1.4, 0.2], [1.0, 0.1],  # 第6个点也是异常\n])\n\n# 聚成2类（正常/异常）\nkmeans = KMeans(n_clusters=2, random_state=42, n_init=10)\nkmeans.fit(data)\n\nfor i, label in enumerate(kmeans.labels_):\n    print(f'点{i+1}: {\"⚠️ 异常\" if label == 1 else \"✓ 正常\"}')\n```\n\n聚类模型自动把数据分成了两组——沉降大、速率快的那组被标记为异常，不需要你手动设阈值。\n\n## 四、第三个例子：用决策树做质量分类\n\n### 场景\n\n根据几个简单的指标，判断一条基线是否合格。\n\n```python\nfrom sklearn.tree import DecisionTreeClassifier\n\n# 特征：[RMS(m), Ratio, 基线长度(km)]\nX_train = [\n    [0.008, 5.2, 2.1],    # 合格\n    [0.015, 3.8, 1.5],    # 合格\n    [0.045, 1.5, 2.0],    # 不合格\n    [0.012, 4.1, 3.0],    # 合格\n    [0.060, 1.2, 1.8],    # 不合格\n]\ny_train = ['合格', '合格', '不合格', '合格', '不合格']\n\n# 训练\nclf = DecisionTreeClassifier()\nclf.fit(X_train, y_train)\n\n# 判断新数据\nX_new = [[0.010, 4.5, 2.5]]\nprint(f'预测结果: {clf.predict(X_new)[0]}')\n```\n\n用历史合格/不合格的基线数据训练模型，以后新测的基线就可以自动判断质量等级。\n\n## 五、测量员学机器学习需要什么基础？\n\n| 你需要 | 不需要 |\n|-------|--------|\n| 会Python基础语法 | 高等数学（现用现查即可） |\n| 理解「数据」是什么 | 推导算法公式 |\n| 知道自己的测量场景 | 从零写神经网络 |\n| 会用 pip install 装库 | 理解底层数学原理 |\n\n机器学习库（如 scikit-learn）已经把算法封装好了，你只需要：\n\n```bash\npip install scikit-learn pandas matplotlib\n```\n\n然后像上面例子那样，**准备数据 → 调用模型 → 看结果**。\n\n## 总结\n\n机器学习不是什么黑魔法，它就是**用数据自动找规律**。\n\n- **线性回归**：做预测（沉降预测、变形趋势）\n- **聚类**：找异常（自动标记粗差、异常测点）\n- **决策树**：做分类（基线质量评定、风险分级）\n\n你的测量数据（沉降值、坐标、RMS、Ratio）天然适合机器学习。从最简单的线性回归开始，试试看。\n";
