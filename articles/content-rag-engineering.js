// 文章：rag-engineering
ARTICLE_CONTENTS["rag-engineering"] = "# RAG 知识库工程化：从\"能用\"到\"好用\"\n\n> 把几十份文档切块、向量化、建索引，RAG 助手当天就能\"跑起来\"——这是 demo。但用三个月就会遇到一堆问题：新规范发布了怎么更新？旧文档还在库里，检索总返回过期内容？同一份资料被切进去两次，回答里重复片段？知识库越来越大，每次改一点就要全部重建？这些问题的本质是：**知识库被当成\"一次性导入的静态文件\"，而不是\"需要持续维护的资产\"**。本文讲 RAG 知识库的工程化：多库分层的数据模型、基于文件哈希的增量更新（改哪篇更新哪篇，不重建全库）、文档版本管理与过期过滤、数据质量检查、多库隔离与检索路由，以及日常运维清单，并给出增量更新的完整代码。\n\n## 一、为什么需要工程化\n\n### 1.1 demo 与生产的差距\n\n| 对比维度 | Demo（一次性） | 生产（持续维护） |\n|---|---|---|\n| 建库 | 手动导入一次 | 定期自动同步 |\n| 文档更新 | 重新全量导入 | **增量更新**（改哪篇更哪篇） |\n| 旧版本 | 不管 | 标记作废、过滤 |\n| 重复文档 | 不管 | 去重 |\n| 多来源 | 混在一起 | 分库隔离 |\n| 出错 | 重来 | 可追踪、可回滚 |\n\n### 1.2 常见\"用坏了\"的症状\n\n```text\n症状1：新规范发布了，但问问题还返回旧条文\n  → 没有更新机制\n\n症状2：同一份方案被导入两次，回答里重复段落\n  → 没有去重\n\n症状3：不知道库里到底有哪些文档\n  → 没有目录/清单\n\n症状4：全库重建要半小时，改一个文件也要等\n  → 没有增量更新\n```\n\n> 注意：知识库工程化的核心原则是——**把\"文档\"当成一等公民来管理**（有清单、有版本、有状态），而不是只盯着\"向量\"。\n\n---\n\n## 二、多库分层的数据模型\n\n### 2.1 三层结构\n\n```text\n知识库（assistant）\n├── 规范库（公共，只读）\n│   ├── 公路桥涵施工技术规范（2023版）\n│   └── 工程测量规范（GB 50026）\n├── 项目库（按项目隔离）\n│   ├── 项目A：施工方案、图纸说明、会议纪要\n│   └── 项目B：……\n└── 个人库（笔记、经验）\n    ├── 我的测量笔记\n    └── 收藏的文章\n```\n\n### 2.2 文档元数据（每篇必带）\n\n```python\ndoc = {\n    \"doc_id\": \"std_jtg_2023\",       # 文档唯一 ID\n    \"title\": \"公路桥涵施工技术规范\",\n    \"version\": \"2023\",              # 版本\n    \"source\": \"规范/项目A/笔记\",     # 所属库\n    \"status\": \"active\",             # active / deprecated（作废）\n    \"updated\": \"2026-08-01\",        # 最后更新\n    \"hash\": \"a3f9...\",              # 内容哈希（增量更新用）\n}\n```\n\n**检索时强制过滤 `status=active`**——作废文档不返回（同图纸版本守卫的思路）。\n\n---\n\n## 三、增量更新（核心）\n\n### 3.1 原理：文件哈希检测\n\n不比较内容本身（慢），比较**哈希值**（快）：\n\n```text\n扫描资料文件夹\n  ↓\n对每篇文档计算哈希（MD5）\n  ↓\n与数据库记录对比：\n  ├─ 新文档（库里没有）→ 入库\n  ├─ 哈希变了（文档更新）→ 重新切块+向量化，替换旧条目\n  └─ 哈希没变 → 跳过\n  ├─ 库里有多但文件夹没了 → 标记作废（不删，保留审计）\n```\n\n### 3.2 完整实现\n\n```python\nimport hashlib, os, sqlite3, json\n\n# ===== 文档注册表（SQLite）=====\ndb = sqlite3.connect('kb.db')\ndb.execute('''CREATE TABLE IF NOT EXISTS docs (\n    doc_id TEXT PRIMARY KEY,\n    path TEXT, hash TEXT, status TEXT,\n    updated TEXT\n)''')\n\ndef file_hash(path):\n    h = hashlib.md5()\n    with open(path, 'rb') as f:\n        for chunk in iter(lambda: f.read(8192), b''):\n            h.update(chunk)\n    return h.hexdigest()\n\n# ===== 增量同步：扫描 → 对比 → 更新 =====\ndef sync_kb(folder, vectorize_fn):\n    \"\"\"vectorize_fn(doc): 切块+向量化+入库（你现有的管道）\"\"\"\n    current = set()\n    for fname in os.listdir(folder):\n        if not fname.endswith(('.pdf', '.docx', '.txt', '.md')):\n            continue\n        path = os.path.join(folder, fname)\n        doc_id = fname.replace(' ', '_')\n        h = file_hash(path)\n        current.add(doc_id)\n\n        row = db.execute(\"SELECT hash FROM docs WHERE doc_id=?\", (doc_id,)).fetchone()\n        if row is None:\n            # 新文档 → 入库\n            vectorize_fn(path)\n            db.execute(\"INSERT INTO docs VALUES (?,?,?,?,?)\",\n                       (doc_id, path, h, 'active', today()))\n            print(f\"➕ 新增: {fname}\")\n        elif row[0] != h:\n            # 内容变了 → 更新（先删旧向量，再重新入库）\n            delete_vectors(doc_id)        # 删旧向量\n            vectorize_fn(path)            # 重新切块入库\n            db.execute(\"UPDATE docs SET hash=?, updated=? WHERE doc_id=?\",\n                       (h, today(), doc_id))\n            print(f\"🔄 更新: {fname}\")\n        # else: 没变，跳过\n\n    # 文件夹里没有的 → 作废（不删，保留历史）\n    for (doc_id,) in db.execute(\"SELECT doc_id FROM docs WHERE status='active'\"):\n        if doc_id not in current:\n            db.execute(\"UPDATE docs SET status='deprecated' WHERE doc_id=?\", (doc_id,))\n            print(f\"⛔ 作废: {doc_id}（文件已不在）\")\n    db.commit()\n```\n\n运行效果：\n\n```text\n第一次运行：➕ 新增 5 篇（全量入库）\n第二次运行：（无变化）什么都不做，秒级完成\n改了其中一篇：🔄 更新 1 篇（其余跳过）\n删了一篇：   ⛔ 作废 1 篇（向量保留但检索过滤）\n```\n\n> 注意：增量更新的收益随库变大而放大——100 篇文档时全量重建要几十分钟，增量更新只需处理变动的 1~2 篇，几秒钟。\n\n### 3.3 删除旧向量的实现\n\n```python\ndef delete_vectors(doc_id):\n    \"\"\"从向量索引中删除该文档的全部向量（按 ID 范围）\"\"\"\n    ids = db.execute(\"SELECT vec_id FROM doc_vectors WHERE doc_id=?\", (doc_id,)).fetchall()\n    if ids:\n        index.remove_ids(np.array([i[0] for i in ids], dtype='int64'))\n```\n\n**实现前提：向量 ID 要能映射回文档**——建库时给每个向量打上 doc_id 标签，否则无法定点删除。\n\n---\n\n## 四、文档版本管理\n\n### 4.1 同文档多版本\n\n同一规范/方案可能有多版（2023版/2024版），处理原则：\n\n- **同 ID 不同版本**：入库时版本号写进元数据\n- **只留最新版 active**：新版本入库时，旧版本标 deprecated\n- **检索过滤**：`status='active'` 是硬过滤\n\n### 4.2 版本变更提示\n\n检索到内容时，如果文档元数据有\"最近更新\"，回答里可以带：\n\n```text\n\"按《工程测量规范》GB 50026（2020版）……\"\n\"⚠️ 该规范 2026-08 已更新至新版，本回答基于旧版，请核对\"\n```\n\n> 注意：规范类文档**版本必须明确**——测量规范引用错版本，条文号都对不上。版本号写进检索结果，是工程化对测量场景的最低要求。\n\n---\n\n## 五、数据质量\n\n### 5.1 入库前检查\n\n| 检查项 | 规则 | 处理 |\n|--------|------|------|\n| 空文档 | 切块数为 0 | 警告，不入库 |\n| 过短 | < 100 字 | 警告，标记低质量 |\n| 乱码 | 大量异常字符 | 拦截，提示重新导出 |\n| 重复文档 | 内容哈希相同 | 去重（保留一份） |\n\n### 5.2 相似去重（内容级）\n\n```python\ndef dedup_check(text, threshold=0.95):\n    \"\"\"新文档与库内已有文档的相似度检测\"\"\"\n    v = embed([text])[0]\n    scores, ids = index.search(v.reshape(1, -1), 3)\n    return scores[0][0] > threshold   # 太高 → 疑似重复\n```\n\n### 5.3 质量监控\n\n- 定期统计：每库文档数、向量数、失效文档数\n- 检索日志分析：经常检索不到的文档 → 检查切块是否合理\n\n---\n\n## 六、多库隔离与检索路由\n\n### 6.1 向量索引按库分\n\n每个库独立索引（或同一索引用 ID 前缀区分）：\n\n```python\nKB_LIBS = {\n    \"规范库\":   {\"index\": index_std,  \"ids\": range(0, 10000)},\n    \"项目A\":    {\"index\": index_projA, \"ids\": range(10000, 20000)},\n    \"个人库\":   {\"index\": index_personal, \"ids\": range(20000, 30000)},\n}\n```\n\n### 6.2 检索路由\n\n```python\ndef search(question, libs=None):\n    \"\"\"libs=None 全库；libs=['规范库'] 只查规范库\"\"\"\n    targets = [KB_LIBS[l] for l in (libs or KB_LIBS.keys())]\n    results = []\n    for t in targets:\n        scores, ids = t[\"index\"].search(qv, k=5)\n        results.extend(...)\n    return top_k(results)\n\n# 使用\nsearch(\"四等水准限差\", libs=[\"规范库\"])     # 规范问题只查规范库\nsearch(\"3号墩方案\", libs=[\"项目A\"])          # 项目问题只查项目库\nsearch(\"全库查一遍\")                          # 全库\n```\n\n**路由的好处**：项目库资料（保密）不会混进规范问答，规范库也不会被项目噪音污染；权限控制也天然（指定库才可查）。\n\n---\n\n## 七、运维清单\n\n```text\n日常：\n  □ 定时任务：每晚扫描资料文件夹，增量同步（增量更新脚本）\n  □ 新规范发布 → 入库新版本 → 旧版本自动作废\n  □ 检查日志：新增/更新/作废记录\n\n每周：\n  □ 数据质量检查（空文档/重复/乱码）\n  □ 检索抽样测试（每库抽 5 个问题验证）\n\n每月：\n  □ 索引备份（向量索引 + 注册表）\n  □ 检索日志分析（高频查不到的内容 → 补资料或调切块）\n  □ 清理 deprecated 文档的向量（可选，释放空间）\n```\n\n---\n\n## 八、工程化检查清单（对照自查）\n\n- [ ] 每篇文档有元数据（ID/版本/来源/状态/哈希）\n- [ ] 增量更新已实现（改哪篇更哪篇）\n- [ ] 作废文档检索时被过滤\n- [ ] 重复文档已去重\n- [ ] 多库分库（规范/项目/个人），检索可指定库\n- [ ] 版本号进检索结果（测量规范引用要明确版本）\n- [ ] 定时任务自动同步\n- [ ] 有备份和回滚手段\n\n---\n\n## 九、小结\n\n知识库工程化，一句话：**把知识库从\"文件堆\"变成\"有管理的资产\"**。\n\n- **元数据**：每篇文档有身份（版本、状态、哈希）——一切管理的基础\n- **增量更新**：哈希检测，改哪篇更哪篇——维护成本不随库变大\n- **版本与质量**：作废过滤、重复去重——保证检索结果可信\n- **多库隔离**：规范/项目/个人分层——权限与相关性兼得\n\n工程化做完，RAG 助手才真正\"好用\"——新规范当天生效、过期内容不再出现、库再大也秒级更新。这是从\"个人玩具\"走向\"生产力工具\"的必经一步。";
