// 文章：python-programs
ARTICLE_CONTENTS["python-programs"] = "# 测量辅助计算 Python 程序集\n\n> 这 6 个测量辅助计算程序全部由 Python + tkinter 编写，提供可视化界面，适合日常测量内业计算使用。作者：Lvmyth AI\n\n---\n\n## 一、坐标正反算\n\n坐标正算和坐标反算是测量中最基础的计算，本程序将两者整合在一个界面中。\n\n### 坐标正算\n\n已知起点坐标 (X₀, Y₀)、平距 D 和方位角 α，求终点坐标：\n\n```python\nimport math\n\ndef forward_computation(x0, y0, distance, azimuth):\n    \"\"\"坐标正算\"\"\"\n    a_rad = math.radians(azimuth)\n    x = x0 + distance * math.cos(a_rad)\n    y = y0 + distance * math.sin(a_rad)\n    dx = distance * math.cos(a_rad)\n    dy = distance * math.sin(a_rad)\n    return x, y, dx, dy\n```\n\n### 坐标反算\n\n已知两点坐标，求距离和方位角：\n\n```python\nimport math\n\ndef inverse_computation(x1, y1, x2, y2):\n    \"\"\"坐标反算\"\"\"\n    dx = x2 - x1\n    dy = y2 - y1\n    d = math.hypot(dx, dy)\n    if d < 1e-10:\n        return 0, 0\n    a_rad = math.atan2(dy, dx)\n    if a_rad < 0:\n        a_rad += 2 * math.pi\n    azimuth = math.degrees(a_rad)\n    return d, azimuth\n```\n\n> 程序特点：正算/反算双 Tab 切换，实时绘图显示坐标点、测线和方位角弧\n\n---\n\n## 二、垂足公式\n\n计算测点到轴线的垂距（含垂足坐标），判断点在轴线的左侧还是右侧。\n\n### 核心算法\n\n```python\nimport math\n\ndef perpendicular_foot(ax, ay, azimuth, px, py):\n    \"\"\"\n    计算测点 (px,py) 到轴线（过点 (ax,ay)，方位角 azimuth）的垂距\n    返回：垂足坐标 (fx, fy)，垂距 H（正=左侧，负=右侧）\n    \"\"\"\n    v_rad = math.radians(azimuth)\n    dx = px - ax\n    dy = py - ay\n    L = math.hypot(dx, dy)  # 测点到已知点距离\n    J = math.atan2(dy, dx)\n    if J < 0:\n        J += 2 * math.pi\n    S = J - v_rad  # 方位角差\n    H = L * math.sin(S)  # 垂距\n    proj = L * math.cos(S)  # 沿轴线投影距离\n    fx = ax + proj * math.cos(v_rad)  # 垂足 X\n    fy = ay + proj * math.sin(v_rad)  # 垂足 Y\n    return fx, fy, H\n```\n\n> 程序特点：支持随机测点模拟，历史记录，可视化显示轴线、垂线和偏距标注\n\n---\n\n## 三、前方距离交会\n\n已知两个已知点 A、B 的坐标，以及 A→P、B→P 的距离，求 P 点坐标（两解）。\n\n### 核心算法\n\n```python\nimport math\n\ndef distance_intersection(xa, ya, xb, yb, d_ap, d_bp):\n    \"\"\"前方距离交会，返回两个解\"\"\"\n    d_ab = math.hypot(xb - xa, yb - ya)\n    if d_ab <= 0:\n        return None, None\n    # 余弦定理求角 A\n    cos_a = (d_ab**2 + d_ap**2 - d_bp**2) / (2 * d_ab * d_ap)\n    if cos_a < -1 or cos_a > 1:\n        return None, None  # 不构成三角形\n    angle_a = math.acos(max(-1, min(1, cos_a)))\n    az_ab = math.atan2(xb - xa, yb - ya)\n    # 两解（AB 左侧和右侧）\n    xp1 = xa + d_ap * math.sin(az_ab + angle_a)\n    yp1 = ya + d_ap * math.cos(az_ab + angle_a)\n    xp2 = xa + d_ap * math.sin(az_ab - angle_a)\n    yp2 = ya + d_ap * math.cos(az_ab - angle_a)\n    return (xp1, yp1), (xp2, yp2)\n```\n\n> 程序特点：同时计算两解，可视化标注 AB 基线和两个交会点，支持随机示例\n\n---\n\n## 四、弧上点定位\n\n检核测点是否在设计圆弧上，计算偏距 H（正=偏内，负=偏外）。\n\n### 核心算法\n\n```python\nimport math\n\ndef arc_point_location(ox, oy, R, px, py):\n    \"\"\"判断测点 (px,py) 到圆弧 (圆心 (ox,oy)，半径 R) 的偏距\"\"\"\n    dx = px - ox\n    dy = py - oy\n    I = math.hypot(dx, dy)  # 测点到圆心距离\n    H = R - I  # 偏距（正=偏内，负=偏外）\n    if abs(H) < 0.001:\n        direction = \"在圆弧上\"\n    elif H > 0:\n        direction = f\"偏内 {abs(H):.4f}m（向弧外移）\"\n    else:\n        direction = f\"偏外 {abs(H):.4f}m（向圆心移）\"\n    return I, H, direction\n```\n\n> 程序特点：支持随机测点模拟，可视化显示圆弧、测点和偏距方向\n\n---\n\n## 五、弦长支距\n\n已知圆弧半径 R 和弦长 D，计算任意位置 L 处的拱高 H。\n\n### 核心算法\n\n```python\nimport math\n\ndef chord_offset(R, D, L):\n    \"\"\"弦长支距计算：已知半径 R，弦长 D，求位置 L 处的拱高 H\"\"\"\n    if R <= 0 or D <= 0 or L < 0 or L > D:\n        return None\n    # 弦中点拱高\n    H_mid = R - math.sqrt(R**2 - (D/2)**2)\n    # 任意位置 L 处的拱高\n    H = math.sqrt(R**2 - (D/2 - L)**2) - math.sqrt(R**2 - (D/2)**2)\n    return H_mid, H\n```\n\n> 程序特点：拖动滑条实时计算，可视化显示圆弧、弦和拱高\n\n---\n\n## 六、多边形面积计算\n\n用坐标法（鞋带公式）计算多边形面积，支持任意多边形。\n\n### 核心算法\n\n```python\nimport math\n\ndef polygon_area(points):\n    \"\"\"\n    坐标法计算多边形面积（鞋带公式 / Shoelace formula）\n    points: [(x1,y1), (x2,y2), ..., (xn,yn)]\n    \"\"\"\n    n = len(points)\n    if n < 3:\n        return 0, 0\n    area = 0.0\n    perimeter = 0.0\n    for i in range(n):\n        x1, y1 = points[i]\n        x2, y2 = points[(i + 1) % n]\n        area += x1 * y2 - x2 * y1\n        perimeter += math.hypot(x2 - x1, y2 - y1)\n    area = abs(area) / 2.0\n    return area, perimeter\n```\n\n> 程序特点：点击画布添加顶点，拖拽调整，自动计算面积和周长，顶点列表实时更新\n\n---\n\n## 总结\n\n| 程序 | 功能 | 核心公式 |\n|------|------|----------|\n| 坐标正反算 | 正算/反算坐标 | sin/cos, atan2 |\n| 垂足公式 | 测点到轴线垂距 | sin(方位角差) |\n| 前方距离交会 | 已知两距离求交点 | 余弦定理 |\n| 弧上点定位 | 检核测点是否在弧上 | 圆方程 |\n| 弦长支距 | 任意位置拱高 | 勾股定理 |\n| 多边形面积 | 坐标法算面积 | 鞋带公式 |\n\n> 所有程序均可在 Python 3 环境下运行（需安装 tkinter），作者 Lvmyth AI";
