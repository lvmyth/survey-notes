// 文章：python-survey-log
ARTICLE_CONTENTS["python-survey-log"] = "# Excel处理太慢了？Python自动处理观测手簿\n\n> 做测量内业的都知道，每天回来对着Excel录入观测手簿——桩号、角度、距离、高差，手动校核闭合差，一条条改，一页页算。3个小时的工作量，其实15分钟的脚本就能搞定。\n\n## 一、痛点：你在Excel里浪费了多少时间？\n\n一个典型的测量内业场景：\n\n- 现场记录了50个放样点的观测数据\n- 回到办公室，打开Excel，逐行录入桩号、水平角、平距、高差\n- 手动计算坐标增量，累加，校核闭合差\n- 发现闭合差超限，回头检查哪条数据录错了\n- 改完重新算一遍\n- 最后手动生成报表\n\n这套流程熟练工也要2-3小时，而且**每一步都可能出错**——输错一个小数点、选错一行公式、忘记更新引用范围。\n\n## 二、Python方案：批量处理 + 自动校核\n\n用Python处理观测手簿，核心就两步：**读数据 → 算结果**。\n\n### 1. 读取观测手簿\n\n假设你的观测手簿存为 CSV 格式，结构如下：\n\n```\n测站,后视点,前视点,水平角(°),平距(m),高差(m)\nA1,B1,B2,120.3050,82.468,-1.235\nB1,B2,B3,185.1540,76.312,0.847\nB2,B3,B4,95.2830,91.556,-0.432\n```\n\nPython 读取：\n\n```python\nimport pandas as pd\n\ndf = pd.read_csv('观测手簿.csv', encoding='utf-8')\nprint(df.head())\n```\n\n### 2. 自动计算坐标\n\n```python\nimport math\n\ndef compute_coords(df, start_x, start_y, start_z):\n    \"\"\"从观测手簿逐站推算坐标\"\"\"\n    x, y, z = start_x, start_y, start_z\n    results = [{'测站': '起点', 'X': x, 'Y': y, 'Z': z}]\n\n    for _, row in df.iterrows():\n        angle = math.radians(row['水平角(°)'])\n        dist = row['平距(m)']\n        dh = row['高差(m)']\n\n        # 坐标增量\n        dx = dist * math.cos(angle)\n        dy = dist * math.sin(angle)\n        x += dx\n        y += dy\n        z += dh\n\n        results.append({\n            '测站': row['前视点'],\n            'X': round(x, 3),\n            'Y': round(y, 3),\n            'Z': round(z, 3)\n        })\n\n    return results\n```\n\n### 3. 自动校核闭合差\n\n```python\ndef check_closure(results, known_x, known_y, known_z):\n    \"\"\"与已知点对比，计算闭合差\"\"\"\n    last = results[-1]\n    dx = last['X'] - known_x\n    dy = last['Y'] - known_y\n    dz = last['Z'] - known_z\n    planar = math.hypot(dx, dy)\n    print(f'平面闭合差: {planar:.3f} m')\n    print(f'高程闭合差: {dz:.3f} m')\n    print(f'相对闭合差: 1/{planar:.0f}' if planar > 0 else '平面闭合差为0')\n    return planar, dz\n```\n\n### 4. 导出成果报表\n\n```python\nimport csv\n\ndef export_report(results, filename='测量成果表.csv'):\n    with open(filename, 'w', encoding='utf-8-sig', newline='') as f:\n        writer = csv.DictWriter(f, fieldnames=['测站', 'X', 'Y', 'Z'])\n        writer.writeheader()\n        writer.writerows(results)\n    print(f'已导出: {filename}')\n```\n\n## 三、完整脚本：一键处理\n\n把上面的功能整合成一个完整的脚本：\n\n```python\nimport pandas as pd\nimport math\nimport csv\n\ndef process_survey_log(input_file, start_x, start_y, start_z,\n                       check_x=None, check_y=None, check_z=None):\n    # 1. 读取数据\n    df = pd.read_csv(input_file, encoding='utf-8')\n\n    # 2. 推算坐标\n    results = compute_coords(df, start_x, start_y, start_z)\n\n    # 3. 闭合差校核\n    if check_x is not None:\n        check_closure(results, check_x, check_y, check_z)\n\n    # 4. 导出报表\n    export_report(results)\n\nif __name__ == '__main__':\n    process_survey_log(\n        '观测手簿.csv',\n        start_x=50000.000, start_y=30000.000, start_z=10.000,\n        check_x=50235.182, check_y=30245.631, check_z=14.680\n    )\n```\n\n运行 `python survey_processor.py`，一秒钟出结果。\n\n## 四、进阶：从Excel直接读取\n\n如果数据存在 Excel 里（.xlsx），也很简单：\n\n```python\n# 读取 Excel\ndf = pd.read_excel('观测手簿.xlsx', sheet_name='Sheet1')\n\n# 或者读取特定区域\ndf = pd.read_excel('观测手簿.xlsx', sheet_name='Sheet1',\n                   skiprows=2, usecols='A:E')\n```\n\n`pandas` 库会自动处理 Excel 的格式、合并单元格、空行等问题。\n\n## 五、避坑指南\n\n- **编码问题**：CSV 文件统一用 `utf-8-sig` 编码，避免 Excel 打开乱码\n- **角度格式**：观测手簿中的度分秒建议先转为十进制度，或者用 Python 解析度分秒字符串\n- **数据校验**：脚本跑完后，用已知点坐标反算验证，确保计算逻辑正确\n- **备份原始数据**：在脚本处理前，保留原始观测手簿的副本，方便追溯\n\n## 总结\n\n用 Python 处理观测手簿，不是要你成为程序员，而是把那些重复、机械、容易出错的工作交给代码。\n\n- 以前：Excel 手动录入 + 公式计算 + 逐行校核 = 3小时\n- 现在：CSV 导出 + 运行脚本 + 查看报表 = 15分钟\n\n省下来的2个多小时，用来干什么都行。\n";
