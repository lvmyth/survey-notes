// 文章：multimodal-rag
ARTICLE_CONTENTS["multimodal-rag"] = "# 多模态 RAG：让测量助手会\"看\"\n\n> 测量员的资料库里，文字只占一半：另一半是手写的观测手簿、扫描版的规范图纸、现场拍摄的隐蔽工程照片。普通 RAG 只能处理文本，遇到\"这张手簿上 K1+120 的读数是多少\"\"这份扫描规范里四等水准的限差要求\"就抓瞎了。多模态 RAG 要解决的，就是让知识库助手从\"会读字\"变成\"会看图\"。本文讲解测量场景多模态 RAG 的两条技术路线——**OCR 路线**（图片变文字，走成熟管道）与**视觉模型路线**（多模态大模型直接看图），给出 PaddleOCR 识别手簿与扫描规范的完整实战代码，设计\"文字型图片走 OCR、图形型图片走视觉模型\"的混合架构，并讨论手写识别、表格结构、图纸处理等测量特有的难点。\n\n## 一、为什么测量场景必须多模态\n\n### 1.1 测量资料的非文本占比\n\n| 资料 | 形态 | 普通 RAG 能处理吗 |\n|------|------|-----------------|\n| 观测手簿（纸质手写） | 图片/扫描件 | ❌ |\n| 规范扫描版（老规范） | 图片 PDF | ❌ |\n| 施工图纸（图签/说明） | CAD/PDF | ❌（只能处理内嵌文字） |\n| 现场照片（隐蔽工程/断面） | 图片 | ❌ |\n| 电子文档（Word/文本PDF） | 文本 | ✅ |\n\n### 1.2 痛点场景\n\n```text\n\"这张水准手簿 B 点的读数是多少？\"\n  → 图片，普通 RAG 答不了\n\n\"扫描版规范里，四等水准的视线长度限值？\"\n  → 图片 PDF，切块全是乱码\n\n\"这个断面照片对应的桩号范围？\"\n  → 需要看图理解\n```\n\n> 注意：测量资料\"一半是图\"不是夸张——纸质手簿、图纸、现场照片是测量员日常的三大非文本载体。多模态不是锦上添花，是补齐另一半资料库。\n\n---\n\n## 二、两条技术路线\n\n### 2.1 路线对比\n\n| 维度 | **OCR 路线** | **视觉模型路线** |\n|---|---|---|\n| 原理 | 图片 → 文字 → 普通 RAG | 多模态大模型直接看图回答 |\n| 代表 | PaddleOCR（本地免费） | Qwen-VL（本地）/ GPT-4V（API） |\n| 适合 | **文字密集**（手簿、扫描规范） | **图形理解**（图纸、照片内容） |\n| 精度 | 印刷体高，手写一般 | 图文都行，但细节易错 |\n| 成本 | 本地免费 | token 贵 |\n| 可检索性 | ✅ 转成文字后可向量检索 | ❌ 每次都要重新看图 |\n\n### 2.2 核心判断\n\n- **文字型图片**（手簿、扫描件、表格照）→ **OCR 转文本**，入库走标准 RAG——可检索、可复用、免费\n- **图形型图片**（图纸布局、照片内容、示意图）→ **视觉模型**直接理解\n\n**测量场景 90% 的需求是\"图片里的文字和数据\"**——OCR 路线是主力，视觉模型是补充。\n\n---\n\n## 三、OCR 实战：PaddleOCR 识别手簿与扫描规范\n\n### 3.1 安装\n\n```bash\npip install paddlepaddle paddleocr\n```\n\n（首次运行会自动下载模型，之后可离线）\n\n### 3.2 识别手簿\n\n```python\nfrom paddleocr import PaddleOCR\n\nocr = PaddleOCR(use_angle_cls=True, lang='ch')   # 中文识别\n\ndef ocr_image(img_path):\n    result = ocr.ocr(img_path, cls=True)\n    lines = []\n    for res in result:\n        if not res:\n            continue\n        for line in res:\n            text = line[1][0]          # 识别文本\n            conf = line[1][1]          # 置信度\n            lines.append((text, round(conf, 3)))\n    return lines\n\n# 识别水准手簿照片\nlines = ocr_image(\"handbook.jpg\")\nfor text, conf in lines[:10]:\n    print(f\"{conf:.2f}  {text}\")\n```\n\n输出示例：\n\n```text\n0.98  水准观测记录表\n0.97  测站  后视  前视  高差\n0.95  B1   1.254  1.108  +0.146\n0.93  B2   1.215  1.221  -0.006\n```\n\n### 3.3 OCR 后处理管道（关键一步）\n\nOCR 识别结果**不能直接入库**——要清洗、结构化、切块：\n\n```python\ndef clean_ocr_lines(lines, min_conf=0.85):\n    \"\"\"过滤低置信度、去噪、合并\"\"\"\n    clean = []\n    for text, conf in lines:\n        if conf < min_conf:\n            continue                      # 低置信度丢弃或标记\n        text = text.replace(' ', '').strip()\n        if len(text) >= 2 and not text.isdigit():\n            clean.append(text)\n    return clean\n\ndef handbook_to_chunks(lines):\n    \"\"\"手簿 → 语义块（每行一条，带表头上下文）\"\"\"\n    header = lines[0] if lines else \"水准记录\"\n    chunks = []\n    for line in lines[1:]:\n        if any(k in line for k in [\"测站\", \"后视\", \"前视\"]):\n            header = line                     # 表头更新\n        else:\n            chunks.append(f\"水准记录 {header}：{line}\")\n    return chunks\n\nchunks = handbook_to_chunks(clean_ocr_lines(lines))\n# chunks 直接进入向量化 → 入库（与文本 RAG 完全一致）\n```\n\n> 注意：手簿/表格的 OCR 后处理比印刷文本要求高——**表头要跟随行数据**（否则\"1.254\"不知道是后视还是前视）、置信度过滤、数字与文字分开处理。\n\n### 3.4 扫描规范入库\n\n```python\n# PDF 扫描件 → 逐页 OCR → 合并文本 → 切块入库\nimport fitz  # PyMuPDF\n\ndef pdf_scan_to_text(pdf_path):\n    doc = fitz.open(pdf_path)\n    pages = []\n    for page in doc:\n        pix = page.get_pixmap(dpi=200)\n        img_path = f\"/tmp/page_{page.number}.png\"\n        pix.save(img_path)\n        lines = ocr_image(img_path)\n        pages.append(\"\\n\".join(t for t, _ in lines))\n    return \"\\n\\n\".join(pages)\n```\n\n扫描规范 OCR 后，整本规范变成可检索文本——与《本地部署RAG测量助手》的管道无缝衔接。\n\n---\n\n## 四、视觉模型路线（图形理解）\n\n### 4.1 本地 Qwen-VL\n\n```python\n# 用 ollama 跑多模态模型（需要先 ollama pull qwen2.5vl）\nimport requests, base64\n\ndef ask_image(img_path, question):\n    with open(img_path, \"rb\") as f:\n        b64 = base64.b64encode(f.read()).decode()\n    resp = requests.post(\"http://localhost:11434/api/chat\", json={\n        \"model\": \"qwen2.5vl:7b\",\n        \"messages\": [{\n            \"role\": \"user\",\n            \"content\": question,\n            \"images\": [b64],\n        }],\n        \"stream\": False,\n    })\n    return resp.json()[\"message\"][\"content\"]\n```\n\n### 4.2 适合问什么\n\n```text\n\"这张断面照片里，掌子面有没有明显超挖？\"（照片理解）\n\"这张图纸上标注的坡度是多少？\"（图形+文字）\n\"这个隐蔽工程照片对应哪个部位？\"（内容判断）\n```\n\n### 4.3 局限\n\n- 视觉模型对**精确数值**（手簿读数）识别不如 OCR + 后处理稳\n- 看图问答每次都要传图，token 成本高、不可检索\n- 本地视觉模型（7B 级）复杂图纸理解有限\n\n> 注意：视觉模型适合\"看一次\"的图，不适合\"入库反复查\"的图。测量资料里需要反复查的文字型图片，一律走 OCR 入库。\n\n---\n\n## 五、混合架构：测量助手的多模态设计\n\n```text\n用户上传/提问带图片\n  ├─ 路由判断（文件名/问题关键词/图片类型）：\n  │   ├─ 文字型（手簿/记录表/扫描规范）→ OCR → 文本入库/检索\n  │   └─ 图形型（图纸/照片/示意图）    → 视觉模型直接理解\n  ├─ OCR 结果 → 清洗 → 切块 → 向量化 → 进知识库（一次入库，反复检索）\n  └─ 回答时：\n      检索到的手簿数据 → 表格/文本回答\n      图纸问题 → 视觉模型看图 + 上下文\n```\n\n**关键设计：OCR 入库是\"一次性投资，反复收益\"；视觉模型是\"按次付费\"**——能用 OCR 的先 OCR，视觉模型兜底。\n\n---\n\n## 六、测量场景示例\n\n### 场景 1：手簿数字化入库\n\n```text\n拍照 → OCR → 结构化（测站/后视/前视/高差）→ 入库\n之后提问：\"B1 测站的高差是多少？\" → 检索命中 → 表格回答\n```\n\n### 场景 2：老规范扫描件\n\n```text\n扫描版规范 → 逐页 OCR → 全文可检索\n提问：\"四等水准视线长度限值？\" → 命中条文\n```\n\n### 场景 3：图纸说明提取\n\n```text\nCAD 图纸 → 导出图签/说明文字 → 入库\n提问：\"QL-01 图签的版本和日期？\" → 命中图签信息\n（图形部分由视觉模型按需看，不入库）\n```\n\n---\n\n## 七、防坑清单\n\n| 坑 | 对策 |\n|---|---|\n| 手写识别不准 | 置信度过滤 + 人工抽检；关键读数双人核对 |\n| 表格 OCR 结构乱 | 表头跟随行、按行结构化、模板匹配 |\n| 扫描件倾斜/模糊 | OCR 前预处理（纠偏、增强）、提高 DPI |\n| OCR 文本入库质量差 | 清洗管道（去噪/去重/过滤低置信度） |\n| 把图形型图片硬塞 OCR | 路由判断：图形走视觉模型 |\n| 视觉模型 token 烧钱 | 文字型一律 OCR 入库，视觉模型只处理\"看一次\"的图 |\n| 敏感图纸外传 | 本地 OCR + 本地视觉模型，不上传云端 |\n\n---\n\n## 八、小结\n\n多模态 RAG 对测量助手的意义一句话：**把占资料库一半的图片变成可检索的知识**。\n\n- **主力是 OCR**：手簿、扫描规范、表格照——转文本入库，一次投资反复检索\n- **补充是视觉模型**：图纸理解、照片判断——按需看图\n- **关键是路由**：文字型走 OCR、图形型走视觉模型，别用错\n- **测量特色**：手写识别、表格结构化、图签提取——后处理管道比 OCR 本身更重要\n\n做完多模态，你的测量助手才真正覆盖了测量资料的全形态：文字（规范/方案）+ 表格（数据）+ 图片（手簿/图纸/照片）——这才是一个完整的测量知识库。";
