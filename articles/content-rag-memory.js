// 文章：rag-memory
ARTICLE_CONTENTS["rag-memory"] = "# 给 RAG 助手装上记忆：从问答历史到长期偏好\n\n> RAG 助手用久了会有一个尴尬：用户昨天问过\"四等水准的视线长度是多少\"，今天换个说法又问一遍，助手老老实实重新检索、重新回答——好像什么都没发生过。再往后，用户说\"按我们上次那个项目的要求算\"，助手一脸茫然，因为它根本不知道\"上次那个项目\"是什么。问题出在：RAG 助手只有\"知识\"，没有\"记忆\"。知识库是公共的、不变的（规范、文档），记忆是个人的、累积的（你问过什么、常用什么、做到哪了）。本文给 RAG 助手加记忆：先分清记忆的三个层次（会话上下文、问答历史、长期偏好），再给出\"问答历史入库 + 向量记忆检索\"的完整实现（SQLite 存储 + Faiss 记忆索引 + Agent 流程增强），讨论阈值、容量、时间衰减等调优参数，最后谈进阶方向（偏好档案、知识沉淀）和测量场景特有的\"旧答案复用陷阱\"。\n\n## 一、记忆的三个层次\n\n| 层次 | 记什么 | 生命周期 | 你的助手现状 |\n|------|--------|---------|-------------|\n| **① 会话上下文** | 本次对话内容（\"刚才那个 K1+120\"） | 单次会话 | ✅ 已有（Agent 循环的 messages） |\n| **② 问答历史** | 问过的问题 + 回答 + 用过的工具 | 跨会话，长期 | ❌ 本文实现 |\n| **③ 长期偏好** | 常用仪器、项目、规范、习惯 | 跨会话，长期 | ❌ 进阶方向 |\n\n```text\n用户问：这问题以前问过吗？\n  ↓\n① 会话上下文：本会话内 → 直接理解指代\n② 问答历史：跨会话 → 检索记忆库，命中直接复用\n③ 长期偏好：回答时注入（按用户习惯定制）\n```\n\n> 注意：三个层次不是互斥，而是叠加——会话上下文管\"这次聊到哪\"，问答历史管\"以前问过啥\"，长期偏好管\"这个人喜欢怎样\"。成熟的助手三层都有。\n\n## 二、为什么助手需要记忆\n\n1. **重复问题秒回**：同样的问题不再重复检索+生成，省时间省 token\n2. **跨会话连续**：上次算到哪、上次的结论是什么，用户不用重新说\n3. **个性化回答**：知道用户常用什么仪器、什么项目，回答更贴\n4. **知识沉淀**：好问题好答案累积成个人知识资产\n\n测量场景尤其明显：现场重复问规范数值、反复算同一类数据、同一个项目多次咨询——记忆能把\"每次都从头来\"变成\"直接给上次结果\"。\n\n---\n\n## 三、方案设计：问答历史 + 记忆检索\n\n```text\n用户提问\n  ↓\n① 先检索\"记忆库\"（历史问答向量）：这问题以前问过吗？\n  ├─ 命中（相似度 ≥ 阈值）→ 直接给旧答案 + \"你之前问过\"\n  └─ 未命中 → 正常 RAG/Agent 流程\n  ↓\n② 回答后：问题 + 答案 + 用过的工具 → 存 SQLite\n  ↓\n③ 同时向量化入\"记忆索引\"（Faiss）→ 下次可检索\n```\n\n**关键设计：记忆库与知识库分离**\n\n- **知识库**：规范、文档、博客（公共知识，不变）\n- **记忆库**：你的问答历史（个人记忆，持续增长）\n\n两者分开，互不污染——检索知识库回答规范问题，检索记忆库回答\"我以前问过什么\"。\n\n---\n\n## 四、实现：SQLite + Faiss 记忆索引\n\n### 4.1 存储结构（SQLite）\n\n```python\nimport sqlite3, json\nfrom datetime import datetime\n\ndb = sqlite3.connect('assistant.db')\ndb.execute('''CREATE TABLE IF NOT EXISTS qa_history (\n    id INTEGER PRIMARY KEY AUTOINCREMENT,\n    question TEXT,          -- 用户问题原文\n    answer TEXT,            -- 助手回答\n    tools_used TEXT,        -- 用过的工具（JSON 数组）\n    timestamp TEXT          -- 提问时间\n)''')\n```\n\n### 4.2 记忆索引（Faiss）\n\n```python\nimport faiss, numpy as np\n\n# 用你现有的 embedding 模型（bge-small-zh，512 维）\nmemory_questions = []                          # 历史问题原文（与索引对应）\nmemory_index = faiss.IndexFlatIP(512)          # 内积（归一化后=余弦相似度）\n```\n\n### 4.3 保存：回答后记下来\n\n```python\ndef save_qa(question, answer, tools=None):\n    # 存 SQLite\n    db.execute(\"INSERT INTO qa_history (question, answer, tools_used, timestamp) VALUES (?,?,?,?)\",\n               (question, answer, json.dumps(tools or []), datetime.now().isoformat()))\n    db.commit()\n    # 向量化入记忆索引\n    qv = model.encode([question], normalize_embeddings=True)\n    qv = np.array(qv, dtype='float32')\n    memory_index.add(qv)\n    memory_questions.append(question)\n```\n\n> 注意：**只有\"成功回答\"才入库**——算错了、答非所问的对话不要进记忆，否则会把错误答案反复复用。可以在回答里带个质量标记，质量低的不存。\n\n### 4.4 检索：提问前先查记忆\n\n```python\ndef recall(question, threshold=0.88, k=3):\n    if memory_index.ntotal == 0:\n        return None\n    qv = model.encode([question], normalize_embeddings=True)\n    qv = np.array(qv, dtype='float32')\n    scores, ids = memory_index.search(qv, k)\n\n    if scores[0][0] >= threshold:              # 相似度够高才复用\n        row = db.execute(\"SELECT answer FROM qa_history WHERE id=?\",\n                         (int(ids[0][0]) + 1,)).fetchone()   # id 从 1 开始\n        if row:\n            return scores[0][0], row[0]\n    return None\n```\n\n### 4.5 Agent 主流程增强\n\n```python\ndef agent_with_memory(question):\n    hit = recall(question)\n    if hit:\n        score, old_answer = hit\n        return (f\"📌 这个问题你之前问过（相似度 {score:.2f}）：\\n\"\n                f\"{old_answer}\\n\\n\"\n                f\"⚠️ 如需按最新数据重新计算，请说\\\"重新算\\\"\")\n    answer = agent(question)                   # 你现有的 RAG/Agent 流程\n    save_qa(question, answer, last_tools)\n    return answer\n```\n\n运行效果：\n\n```text\n用户第一次：四等水准视线长度限值？\n助手：≤100m，前后视距差≤3m（完整回答）\n\n用户第二次（换说法）：水准测量视线最多多长？\n助手：📌 这个问题你之前问过（相似度 0.93）：\n      ≤100m，前后视距差≤3m\n      ⚠️ 如需按最新数据重新计算，请说\"重新算\"\n```\n\n---\n\n## 五、调优参数\n\n| 参数 | 作用 | 建议 |\n|------|------|------|\n| **threshold** | 多相似算\"问过\" | 0.85~0.9：太低乱复用，太高记不住 |\n| **记忆条数上限** | 防膨胀 | 500~1000 条滚动清理（删最旧） |\n| **时间衰减** | 旧记忆降权 | 30 天前的问题相似度 ×0.9，90 天 ×0.7 |\n| **工具相关记忆** | 带工具的记忆权重更高 | 平差/计算类问题复用价值高 |\n| **失败不入库** | 质量过滤 | 只存成功回答 |\n\n时间衰减实现：\n\n```python\ndef recall_with_decay(question, threshold=0.88):\n    hit = recall(question, threshold=threshold)\n    if not hit:\n        return None\n    score, answer = hit\n    row = db.execute(\"SELECT timestamp FROM qa_history WHERE question=? ORDER BY id DESC LIMIT 1\",\n                     (question,)).fetchone()\n    if row:\n        days = (datetime.now() - datetime.fromisoformat(row[0])).days\n        if days > 30:\n            score *= 0.9          # 旧记忆降权\n            if score < threshold:\n                return None        # 太旧且不够像 → 重新回答\n    return score, answer\n```\n\n---\n\n## 六、进阶方向\n\n### 6.1 长期偏好档案\n\n定期（如每 10 次对话）用 LLM 总结对话 → 提取偏好档案：\n\n```json\n{\n  \"常用仪器\": \"徕卡全站仪\",\n  \"常查规范\": \"公路桥涵施工技术规范\",\n  \"当前项目\": \"某隧道贯通测量\",\n  \"语言习惯\": \"偏好度分秒格式\"\n}\n```\n\n回答时把档案注入 System Prompt，回答自动贴合用户习惯。\n\n### 6.2 知识沉淀\n\n用户对某个回答点了\"有用\" → 问题+答案入**知识库**（不只是记忆库）：\n\n- 记忆库：个人历史，可删\n- 知识库：沉淀的知识，长期有效、可检索\n- 一键转存：把好问答变成知识库新条目\n\n### 6.3 记忆管理\n\n- 查看/删除历史（测量资料有保密需求，用户应能清空）\n- 导出问答记录（归档）\n- 多项目隔离：按项目分记忆空间\n\n---\n\n## 七、测量场景的特殊注意：旧答案复用陷阱\n\n给测量助手加记忆，有一个其他领域没有的坑——**测量数据会过期**：\n\n```text\n用户昨天问：\"K1+120 的设计高程？\" → 记下来：45.350\n今天又问同样问题 → 直接给 45.350\n但如果设计变更了，旧答案就是错的！\n```\n\n对策：\n\n1. **数据类答案标注时效**：\"此为 2026-08-23 查询结果，变更后请重新计算\"\n2. **计算类答案强制重算**：平差、坐标、填挖这类结果，记忆只存\"问过\"，不存\"结果\"\n3. **变更提示**：如果知识库检测到相关文档更新，记忆命中时提示\"相关资料已更新\"\n\n```python\n# 计算类问题：记忆里只标记\"问过\"，不直接复用结果\nCALC_KEYWORDS = [\"计算\", \"平差\", \"坐标\", \"高程\", \"填挖\", \"反算\", \"正算\"]\ndef agent_with_memory(question):\n    is_calc = any(k in question for k in CALC_KEYWORDS)\n    if not is_calc:                       # 规范/知识类 → 可复用记忆\n        hit = recall(question)\n        if hit:\n            return \"你之前问过：\" + hit[1]\n    answer = agent(question)              # 计算类 → 每次重算\n    save_qa(question, answer, last_tools)\n    return answer\n```\n\n> 注意：**规范数值类可以复用，计算数据类必须重算**——这是测量助手记忆功能的红线。\n\n---\n\n## 八、小结\n\n给 RAG 助手装记忆，本质是给它加了\"经历\"：\n\n- **会话上下文**让它懂\"刚才说的\"（已有）\n- **问答历史**让它记得\"你问过什么\"（本文实现：SQLite + Faiss 记忆索引）\n- **长期偏好**让它懂\"你是怎样的人\"（进阶：偏好档案）\n\n记忆的威力在于：重复问题秒回、跨会话连续、回答个性化、知识随用沉淀。但测量场景要守住一条红线：**规范知识可以复用，计算数据必须重算**——记忆给效率，重算保安全。\n\n> 相关文章：本文是 RAG 系列记忆篇，承接《本地部署RAG测量助手》《RAG进阶》（记忆检索即向量检索的应用）、《让RAG测量助手会算数据》（工具调用与记忆结合）。";
